/// @description mouse_over(startX, startY, width, height);
/// @param startX
/// @param  startY
/// @param  width
/// @param  height
var xx = argument0;
var yy = argument1;
var width = argument2;
var height = argument3;

return(mouse_x >= xx && mouse_x <= width && mouse_y >= yy && mouse_y <= height)
